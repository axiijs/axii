# 05 深度 review（2026-07，已全部修复）

本轮 review 通读了 `src/` 全部源码，对每个疑点先写运行时复现测试（真实 Chromium）确认，再逐项修复。
核心渲染引擎（Host 树、RxListHost 的 splice/reorder/LIS、样式引用计数）未发现新问题；
问题集中在外围 API 层。行级动态 stylesheet 泄漏疑点经实测被证伪（引用计数正确释放）。

回归测试：致命问题在 `__tests__/fatalBugs2.spec.tsx`（F1-F6），改进项在 `__tests__/improvements2.spec.tsx`（I7-I15），编号与下表一致。

## 致命问题修复索引

| # | 问题 | 修复 |
| --- | --- | --- |
| F1 | `createEventTransfer` 的 `target` 作为 ref 使用时，卸载阶段框架以 null 回调（detachRef）命中 "only one target" 检查，`root.destroy()` 必抛错且销毁中断 | `target(null)` 视为解绑，元素重挂载后可再次绑定（`src/eventAlias.ts`） |
| F2 | `onSpaceKey` 比较 `e.key === 'Space'`，真实空格键的 `e.key` 是 `' '`，永远不触发 | 同时匹配 `e.key === ' '` 与 `e.code === 'Space'` |
| F3 | 离场动画实际未发生（离场样式与当前值相同、`display:none`、reduced-motion 等）时，`transitionrun/end` 等待永不 resolve，节点永远留在 DOM | end 等待同时接受 `transitioncancel/animationcancel`；以各元素声明的最长动画时长 + 100ms buffer（上限 10s）作为兜底超时（`src/StaticHost.ts`） |
| F4 | 数组 child 中带 `detachStyle` 的元素被 `StaticArrayHost.destroy` 无条件同步删除，离场动画被跳过 | destroy 尊重子 host 的 `forceHandleElement`，DOM 处理委托给子 host，自己只清理直接创建的文本节点和 placeholder（`src/StaticArrayHost.ts`） |
| F5 | `PropTypes.bool` 用 `typeof v === 'bool'` 校验，恒失败 | 改为 `'boolean'` |
| F6 | `PropTypes.map.stringify` 缺收尾 `}` 且 key 不带引号，产物不是合法 JSON | key 用 `JSON.stringify`，补收尾 `}`，stringify/parse 可往返 |

## 改进项修复索引

| # | 问题 | 修复 |
| --- | --- | --- |
| I7 | `atom(null)`/`atom(undefined)` 作为 child 渲染出字面 `"null"`/`"undefined"` | `stringValue` 对 null/undefined 返回 ''，与函数 child 返回 null 的语义一致（`src/AtomHost.ts`；`functionHost.spec.tsx` 中固化旧行为的断言同步更新） |
| I8 | 响应式 `value` 变为 undefined 时非 text 类型 input 显示 `"undefined"` | 所有 `INPUT` 类型 + `TEXTAREA`，value 为 null/undefined 时置空串（`src/DOM.ts`） |
| I9 | `lazy()` 定义时立即 `load()`（违背代码分割初衷）；load 失败 unhandled rejection 且 UI 永远停在 fallback | load 推迟到首次渲染；rejection 存入 error atom 并传给 `fallback(error)`（`src/lazy.ts`） |
| I10 | `RxDOMRect` 的 `requestAnimationFrame`/`requestIdleCallback` 模式只调度一次，不持续跟踪 | 回调内重新调度形成循环，abort 时停止；顺带实现类型里声明但落到 assert(false) 的 `'manual'` 模式（`src/reactiveDOMState.ts`） |
| I11 | `RxDOMRect`(window) 缺 `top/left/x/y`；`RxDOMSize` 初始值缺 borderBox/contentBox 字段 | 补齐完整类型形状；初始值 width/height 与 ResizeObserver 的 contentRect 语义对齐（content box） |
| I12 | 同一元素上两个 `RxDOMSize` 共用 WeakMap 单值槽互相覆盖，任一注销打死其余 | `resizeTargetToStates` 改为 `WeakMap<el, Set<Atom>>`，全部注销才 unobserve |
| I13 | `Portal` 的 `destroyOnUnmount` prop 声明了但未实现 | 默认卸载即销毁；显式 `false` 时保留 portal 内容（`src/Portal.tsx`） |
| I14 | `ComponentHost.typeIds` 是普通 Map，bindProps/lazy/HOC 动态创建组件时无上限增长且函数被永久 pin 住 | 改为 `WeakMap` + 计数器，id 依旧稳定唯一（`src/ComponentHost.ts`） |
| I15 | automatic runtime 的 `jsx()`/`jsxDEV()` 无 children 时给组件传 `children=[undefined]` | children 为 undefined 时不传该实参（`src/DOM.ts`） |

## 代码审查观察项（后续追加修复，测试同在 `__tests__/improvements2.spec.tsx`）

| # | 问题 | 修复 |
| --- | --- | --- |
| O1 | `root.on('error')` 只覆盖组件 render 与函数节点重算；属性绑定更新 / atom 文本更新 / RxList patch 抛错不经过该钩子（list patch 错误 rethrow 进 data0 computed 变成 unhandled rejection） | 三条路径都先交给 error 钩子：属性/文本绑定跳过本次更新、effect 保持活跃可恢复；list patch 被钩子消费后不再 rethrow，未注册钩子时保持原有行为（`src/StaticHost.ts` / `src/AtomHost.ts` / `src/RxListHost.ts`） |
| O2 | `Host.destroy` 的 `parentHandleComputed` 参数在代码库中不再有以 `true` 传入的起点（所有绑定 effect 均 `detachFromCreationContext`），属遗留复杂度 | 从 `Host` 接口和全部实现/调用点删除该参数，effect 清理无条件执行；以 retained diagnostics 断言 destroy 后无泄漏（`src/Host.ts` 及各 Host 实现） |
| O3 | `setAttribute` 事件解绑传 falsy 会把合并存储（onChange 别名成 input 后与 onInput 同槽）整体删除，无法只解绑其中一个 | `_listeners` 改为「事件名 → 来源（别名前事件名）→ 监听」两级结构：解绑只影响自己来源，事件名下无来源时才移除 proxy；同一来源重复设置为覆盖（重绑）语义（`src/DOM.ts`） |
